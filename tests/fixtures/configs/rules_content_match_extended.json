{
  "rules": [
    {
      "type": "content_match",
      "name": "Validate structured files",
      "enabled": true,
      "patterns": ["**/*.json", "**/*.csv", "**/*.yaml", "**/*.yml", "**/*.toml"]
    }
  ]
}
