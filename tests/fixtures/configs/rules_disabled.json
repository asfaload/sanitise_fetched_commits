{
  "rules": [
    {
      "type": "filename_match",
      "name": "Forbid temporary files",
      "enabled": false,
      "patterns": ["**/*.tmp", "**/tmp/**", "**/temp/**"],
      "action": "forbid"
    }
  ]
}
